# Deploy on Cloudflare Workers

Deploy BarberWalkin with OpenNext on Cloudflare Workers. Although Next.js adapter support can vary by feature, prior applications have already validated this deployment path for the team, and Convex remains responsible for dynamic operational state rather than Cloudflare cache bindings.
